export const environment = {
  production: false,
  apiUrl: 'http://localhost:8000',
  // Set to true to bypass the real API entirely and use in-memory mock data
  // (no backend/MongoDB needed) — handy for quick demos. False = real API.
  useMockAuth: false,
};
